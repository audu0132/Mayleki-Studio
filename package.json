{
  "name": "mayleki-studio-root",
  "private": true,
  "scripts": {
    "vercel-build": "cd Frontend && npm install --include=dev && npm run build"
  },
  "engines": {
    "node": "18.x || 20.x"
  }
  

}
